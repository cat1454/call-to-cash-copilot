# Browser-safe values only. Never add Agora certificates, customer secrets,
# webhook secrets, database URLs, private keys, seed phrases, or RPC secrets.
VITE_DEMO_MODE=true
VITE_API_BASE_URL=http://127.0.0.1:3001
VITE_VOICE_PROVIDER=replay
VITE_AI_PROVIDER=deterministic
