# Shared contracts

Owns the executable Zod schemas, TypeScript DTOs, state enums, API/error contracts, event envelopes, and cross-application constants introduced in Phase 2. `apps/api` and `apps/web` consume this package instead of defining cross-boundary contract strings locally.
