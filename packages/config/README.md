# Shared configuration

Reserved for environment schemas and reusable lint or TypeScript configuration when multiple implemented packages need them. Frontend-specific ESLint and Vite configuration remains in `apps/web`.
